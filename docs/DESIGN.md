# Bean Wiki 디자인 시스템 (SSOT)

> 이 문서는 색·타이포·간격의 사용 원칙입니다. 브랜드 컬러의 데이터 정본은
> `src/design/brand-colors.json`이며, `src/app/globals.css`의 런타임 토큰과
> `/design/colors` 카탈로그는 `npm run check:palette`로 함께 검증합니다.

## 0. 브랜드 컬러 — 커피 라이프사이클 팔레트

브랜드 정체성은 **커피의 여정**에서 옵니다: 체리(빨강) → 생두(초록) → 로스팅
갈변(갈색 스펙트럼) → 크레마/폼. **프라이머리 액센트 = 브라운 슈거 로스트**
(`--brand` = `--roast-medium`). 시각 카탈로그: **`/design/colors`** 페이지.

CSS 토큰명은 컴포넌트가 의존하는 **안정된 기능 API**이고, 브랜드명은 독자가
보는 **표시용 이름**입니다. 코드에서는 브랜드명을 식별자로 사용하지 않으며,
표시용 이름이 바뀌어도 기능 토큰명은 유지합니다. 공개 이름, 고정 기준 HEX,
다크 테마 어댑터, 실제 소재, 근거 URL과 스토리는
`src/design/brand-colors.json`에서 함께 관리합니다. 기준 HEX는 실제 재료의
공식 표준색이 아닌 브랜드 근사값이며, 공개 스와치는 테마에 따라 뒤집히지
않습니다.

| 토큰 | 표시용 정식 브랜드명 | 라이트 | 다크 | 기능 역할 |
|---|---|---|---|---|
| `--cherry` | 케냐 AA 커피 체리 | `#a03d36` | `#cd7a6e` | 중요 상태·오답 강조 |
| `--cherry-deep` | 카스카라 허스크 버건디 | `#712a28` | `#a75950` | 체리 계열의 깊은 보조 톤 |
| `--green-bean` | 에티오피아 예가체프 그린빈 | `#7d8f69` | `#9db184` | 생두 단계·긍정적 진행 상태 |
| `--parchment` | 드라이 파치먼트 베이지 | `#e3d4b4` | `#d9c8a6` | 옅은 라이프사이클 배경 |
| `--honey` | 옐로 허니 파치먼트 | `#c8a24c` | `#ddbf73` | 허니 프로세스·성취 강조 |
| `--yellow-bourbon` | 브라질 옐로 버번 커피 체리 | `#c79b35` | `#ddb75e` | 공개 산지 팔레트(성숙 체리 장면) |
| `--wet-hulled` | 인도네시아 만델링 그린빈 | `#5f796c` | `#8eaa9c` | 공개 산지 팔레트(습식 탈곡 생두 장면) |
| `--geisha-silver` | 파나마 게이샤 실버그린 | `#a7aa91` | `#c4c8ad` | 공개 산지 팔레트(공식 품종색 아님) |
| `--monsooned-gold` | 몬순드 말라바 스트로 골드 | `#c3a66a` | `#dbc28d` | 공개 산지 팔레트(몬순 처리 생두 장면) |
| `--coffee-chaff` | 커피 채프 토프 | `#a39383` | `#c2b5a7` | 로스팅 부산물 팔레트 |
| `--roast-light` | 퍼스트 크랙 캐러멜 | `#a26a42` | `#c08a5c` | 로스팅 그라데이션의 밝은 지점(Agtron ~85) |
| `--roast-medium` | 브라운 슈거 로스트 | `#7c5233` | `#c89e6f` | UI 프라이머리(Agtron ~55) |
| `--roast-dark` | 다크 초콜릿 크랙 | `#4e3524` | `#8d6844` | 호버·다크 패널 그라데이션(Agtron ~35) |
| `--espresso` | 바닐라빈 블랙 | `#2b1d13` | `#3a2a1c` | 원두 마크·상시 다크 패널 |
| `--brew-amber` | 93° 브루잉 앰버 | `#b47a3b` | `#d19b68` | 공개 잔 팔레트 |
| `--crema` | 에스프레소 크레마 골드 | `#c99b5f` | `#d9b586` | 다크 패널 강조·원두 크리즈 |
| `--foam` | 65° 벨벳 스팀 밀크 | `#f6efe3` | `#efe6d6` | 역상 텍스트·가장 밝은 표면 |
| `--iced-latte` | 아이스 라테 베이지 | `#d8c7af` | `#e5d5bf` | 공개 잔 팔레트 |
| `--cold-brew` | 콜드브루 브라운 | `#3b2b2a` | `#755856` | 공개 잔 팔레트 |
| `--affogato` | 아포가토 멜트 크림 | `#d6b88b` | `#e2c9a5` | 공개 잔 팔레트 |
| `--brand` / `--brand-strong` | 브라운 슈거 로스트(시맨틱 별칭) | roast-medium / dark | 밝힌 값 | UI 프라이머리/호버 |

### 공개 카탈로그 구성과 이름 규칙

확장 소재 42색의 이름·HEX·근거·스토리는 중복 표를 만들지 않고
`src/design/brand-colors.json`만 정본으로 사용합니다.

- 파트별 개수는 소재의 밀도에 따라 **4–12색** 사이에서 달라질 수 있습니다.
- 데스크톱 카탈로그는 **한 줄 4카드**가 기준입니다.
- 한국어 표시명에 기계적인 글자 수 상한을 두지 않습니다. 산지·메뉴·도구와
  그린빈·커피 체리·크레마 같은 관찰 대상을 함께 보여줍니다.
- `강력분`, `딸기잼` 같은 번역형 재료 표제보다 `브레드 플라워`,
  `스트로베리 잼`처럼 카페 메뉴와 패키지에서 익숙한 외래어 조합을 우선합니다.
- CSS로 제목을 자르거나 말줄임하지 않습니다. 실제 한 줄 표시는 브라우저
  하네스가 데스크톱·태블릿·모바일에서 측정합니다.
- 산지·등급·품종명은 공식 고유색을 뜻하지 않습니다. 관찰 대상과 한계를
  스토리에 쓰고, 공개 HEX는 항상 Bean Wiki의 브랜드 근사값으로 다룹니다.

- 로고·헤더 마크·링크·버튼·포커스 링 등 **사이트 수준 액센트는 `--brand`**.
  (과거 `--olive`가 그 역할이었으나, 올리브는 이제 "커피 기초" 분야 액센트로만.)
- `--forest`는 이름과 달리 현재 값이 **딥 에스프레소**(#26190f)입니다 — 다크
  패널 배경 용도(호환을 위해 토큰명 유지).
- Agtron 번호는 대략적 대응입니다. 절대 기준처럼 쓰지 마세요.

### 파생 틴트 규칙 (color-mix)

옅은 배경 틴트는 **파스텔 hex를 새로 만들지 말고** `color-mix`로 토큰에서
파생합니다 — 라이트/다크가 자동으로 맞춰집니다:

```css
/* 예: 콜아웃·노트·표 헤더·호버 배경 */
background: color-mix(in oklab, var(--copper) 9%, var(--paper));
```

- 관례 비율: 배경 틴트 6~10%, 선택 영역·마크 30~45%.
- 콜아웃/knowledge-note/표 헤더/태그·검색 호버가 이 방식입니다. 다크 전용
  배경 오버라이드를 추가하지 마세요(토큰이 이미 테마를 압니다).

### 상시 다크 패널 규칙

테마와 무관하게 **항상 어두워야 하는 패널**(학습 경로 강조 카드 등)은 테마
토큰을 쓰면 다크 모드에서 밝게 뒤집혀 흰 텍스트가 사라집니다. 이런 표면은
라이프사이클 **고정색**(espresso `#2b1d13`~roast-dark `#4e3524` 그라데이션)과
크레마 계열 텍스트(`#d3ac79`, `#f6efe3`)를 직접 사용하세요. 히어로 원과
피처드 카드의 로스팅 라디얼 그라데이션은 토큰 기반(테마 적응)입니다.
그래픽 선(오빗 링 등)은 잉크 고정 rgba 대신 `var(--line)`을 씁니다.

## 1. 색 토큰

| 토큰 | 표시용 정식 브랜드명 | 라이트 | 다크 | 기능 역할 |
|---|---|---|---|---|
| `--paper` | 화이트 페이퍼 필터 | `#f4f0e7` | `#14170f` | 페이지 배경 |
| `--paper-deep` | 사워도우 아이보리 | `#e8e1d4` | `#1b1f16` | 살짝 눌린 면(카드 등) |
| `--cream` | 커피 블라썸 화이트 | `#fbf8f1` | `#1e2318` | 입력·표면(에디터 등) |
| `--ink` | 포터필터 핸들 블랙 | `#1e1b18` | `#ece7da` | 기본 텍스트·제목 |
| `--muted` | 블랙 세서미 그레이 | `#565451` | `#a9a59f` | 보조 텍스트·라벨 |
| `--prose` | — (내부 의미 토큰) | `#3f463f` | `#cfcabc` | 본문 문단 텍스트 |
| `--prose-soft` | — (내부 의미 토큰) | `#454b45` | `#c4c0b3` | 목록 등 약한 본문 |
| `--line` | — (내부 의미 토큰) | `rgba(24,32,25,.16)` | `rgba(236,231,218,.16)` | 구분선·테두리 |

### 분야 액센트 패밀리 (6개, 12개 카테고리가 공유)

| 토큰 | 표시용 정식 브랜드명 | 공유 카테고리 | 콜아웃 톤 |
|---|---|---|---|
| `--olive` | 로스터스 에이프런 그린 | 커피 기초 · 커피와 건강 | `callout-tip` |
| `--sage` | 커피팜 세이지 | 산지와 생두 · 지속가능성과 거래 | — |
| `--copper` | 카퍼 체즈베 | 로스팅 · 커피 역사와 문화 | `callout-warn` |
| `--blue` | 워싱 스테이션 블루 | 추출 · 커피 과학 | `callout-note` |
| `--berry` | 블랙커런트 커핑 베리 | 센서리 · 음료와 레시피 | `callout-important` |
| `--sand` | 오일드 오크 카운터 | 카페와 장비 · 카페 운영과 품질 | — |

액센트는 라이트/다크에서 각각 다른 값을 가집니다(다크에서 더 밝게). 카테고리와
액센트의 매핑은 `src/content/categories.ts`가 정본이며, `check-content`가
문서 `accent`와 분야 `accent`의 일치를 강제합니다.

> **규칙**: 본문/컴포넌트 CSS에서 회색·베이지 계열을 새 hex로 박지 말고 위
> 토큰을 쓰세요. 다크모드 대응이 자동으로 됩니다(토큰 하나만 오버라이드).

## 2. 폰트

| 토큰 | 값 | 용도 |
|---|---|---|
| `--font-sans` | `var(--font-geist-sans), "Pretendard", "Apple SD Gothic Neo", …` | 기본 UI·본문 |
| `--font-mono` | `var(--font-geist-mono), ui-monospace, …` | 라벨·번호·코드 |

- **로딩**: `src/app/layout.tsx`에서 작은 라틴 서브셋인 Geist / Geist Mono만
  `next/font/google`로 자체 호스팅하고, `--font-geist-sans` ·
  `--font-geist-mono` CSS 변수로 노출합니다.
- **라틴/한글 전략**: Geist에는 한글 글리프가 없습니다. 한글은 설치된
  Pretendard → Apple SD Gothic Neo → Malgun Gothic → 시스템 sans 순으로
  선택합니다. 첫 화면에서 수 MB짜리 CJK 웹폰트를 내려받지 않아도 되어
  렌더링 지연과 흐릿한 폰트 교체를 피합니다.
- ⚠️ **주의**: CSS에서 `"Geist"` 같은 **리터럴 이름**을 쓰면 안 됩니다.
  `next/font`는 해시된 패밀리명을 만들고 오직 `var(--font-*)` 변수로만
  노출하므로 항상 변수를 참조하세요.

## 3. 타입 스케일

- 히어로 `h1`: `clamp(38px, 4.4vw, 56px)` / lh 1.18 / ls `-0.035em`
- 문서 타이틀 `.wiki-title h1`: `clamp(30px, 3.8vw, 44px)` / lh 1.22 / ls `-.03em`
- 섹션 헤딩 `.section-heading h2`: `clamp(26px, 2.6vw, 36px)` / ls `-.03em`
- 본문 섹션 제목 `.article-content h2`: 27px / ls `-.025em`
- 본문 `p`: 18px / lh 1.9 / `word-break: keep-all`
- 목록 `li`: 17px / 1.75 · 일반 UI: 16px · 문서 메타/태그: 14px
- 장식용 mono 라벨: **최소 12px** · 정보를 담은 라벨: **최소 13px**

### 한글 줄맞춤 규칙 (강제)

전역 규칙으로 모든 `h1–h4`에 적용됩니다:

```css
h1, h2, h3, h4 {
  word-break: keep-all;      /* 단어(어절) 중간에서 줄 안 끊음 */
  overflow-wrap: break-word; /* 예외적으로 긴 토큰만 줄바꿈 허용 */
  text-wrap: balance;        /* 여러 줄일 때 줄 길이 균형 → 한 글자 고아 방지 */
}
```

- "…들어 있을\n까" 같은 **한 글자 고아 줄**과 어절 중간 끊김을 막는 조합입니다.
- 큰 제목의 음수 자간은 라틴 기준 `-.05em`대에서 한글 가독 기준 `-.02~-.035em`로
  완화되어 있습니다. 새 제목 스타일도 이 범위를 지키세요.
- 라벨성 텍스트의 **최소 크기는 12px**(mono 캡션), 본문성 메타는 14px 이상.

## 4. 간격·레이아웃

- 컨테이너: `.shell` = `width: min(1180px, calc(100% - 48px))`, 가운데 정렬.
- 헤더 높이: 86px(`.site-header`, `.article-header`).
- 리치 블록(콜아웃·토글·표·figure)의 스타일은 `.article-content` 아래에 정의되어
  발행 문서와 에디터(`.editor-surface.article-content`)가 **같은 CSS**를 공유합니다.

## 5. 다크 모드

- `<html data-theme="light|dark">`. `layout.tsx`의 인라인 스크립트가 저장값 →
  시스템 설정 순으로 초기 테마를 정해 플래시를 막습니다(`ThemeToggle`이 토글).
- 규칙: 컴포넌트는 토큰만 쓰고, 다크 값은 `[data-theme="dark"]` **토큰 블록에서
  한 번**만 바꿉니다. 셀렉터마다 다크 오버라이드를 추가하지 마세요(프로즈 색을
  토큰화하며 그런 중복 오버라이드를 제거했습니다).

## 6. 에셋

- 로고/마크: `src/components/bean-logo.tsx`(`BeanMark`) — 인라인 SVG, `currentColor`.
  원두 몸통은 `--espresso`(진한 갈색) 채움 + `--crema` 센터 크리즈(가운데 갈라진
  선)입니다. 과거 폼(크림)색 채움은 원두가 아니라 빈 자리처럼 읽혀 교체했습니다.
  헤더의 작은 마크는 `--brand` 윤곽선만 사용합니다(`.bean-mark-small`).
- 파비콘/아이콘: `src/app/icon.svg`, `src/app/favicon.ico`.
- OG 이미지: `public/og.png`. 홈의 브랜드 스토리 이미지와 공유 카드가 같은
  원두 지식 지도를 사용합니다.
- 외부 CJK 폰트 파일은 저장소에 두지 않습니다. 라틴 Geist 서브셋만
  `next/font`가 빌드 시 자체 호스팅합니다.
